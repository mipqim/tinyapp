<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
      <a class="navbar-brand" href="/urls">TinyApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        </div>
      </div>
      <form class="d-flex" method="POST">
        <% if (!username) { %>
          <input class="form-control" name="username" type="text" placeholder="Username" style="margin-right: 0.5rem;">
          <button class="btn btn-outline-dark" type="submit" formaction="/login">Login</button>
        <% } else { %>
          <div class="navbar container">Logged In As: <%= username %> User!</div>
          <button class="btn btn-outline-dark" type="submit" formaction="/logout">Logout</button>          
        <% } %>
      </form>         
  </nav>
</header>